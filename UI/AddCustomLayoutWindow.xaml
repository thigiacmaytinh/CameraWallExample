<Window x:Class="WpfCameraWall.AddCustomLayoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Custom Window Division"
        Width="660"
        Height="620"
        WindowStartupLocation="CenterOwner"
        Background="#34383e">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox x:Name="TxtName" Grid.Row="0" Grid.Column="1" Height="32" Margin="0,0,0,8" Text="Custom 3x3" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Dimension" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,8" />
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox x:Name="TxtRows" Width="110" Height="32" Text="3" />
            <TextBlock Text=" X " Foreground="White" VerticalAlignment="Center" Margin="8,0" />
            <TextBox x:Name="TxtCols" Width="110" Height="32" Text="3" />
            <Button Content="Build" Width="80" Margin="12,0,0,0" Click="BtnBuild_Click" />
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Window Division" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,8" />
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Joint" Width="90" Margin="0,0,8,0" Click="BtnJoint_Click" />
            <Button Content="Restore" Width="90" Click="BtnRestore_Click" />
        </StackPanel>

        <Border Grid.Row="3" Grid.ColumnSpan="2" Background="#25282d" BorderBrush="#17191c" BorderThickness="1" Padding="0">
            <Canvas x:Name="LayoutCanvas" Background="#25282d" />
        </Border>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
            <Button Content="Save" Width="96" Height="34" Margin="4" Click="BtnSave_Click" />
            <Button Content="Cancel" Width="96" Height="34" Margin="4" Click="BtnCancel_Click" />
        </StackPanel>
    </Grid>
</Window>
